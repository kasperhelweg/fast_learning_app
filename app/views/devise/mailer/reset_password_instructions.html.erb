<% if Rails.env.development? %>
  <p>Hello <%= @resource.email %>!</p>

  <p>Someone has requested a link to change your password, and you can do this through the link below.</p>

  <p><%= link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

  <p>If you didn't request this, please ignore this email.</p>
  <p>Your password won't change until you access the link above and create a new one.</p>

<% else %>
  <p>Hej <%= @resource.email %>!</p>

  <p>Du har bedt om et link til at skifte dit password. Det kan du gøre her:</p>

  <p><%= link_to 'Skift password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

  <p>Hvis ikke du selv har anmodet om denne mail, bedes du ignorere den..</p>
  <p>Dit password vil ikke blive ændret før du benytter ovenstående link</p>

<% end %>





